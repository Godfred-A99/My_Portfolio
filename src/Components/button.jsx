const Button = ({ name, href }) => {
  return (
    <div className="mt-5 pt-5">
      <a
        href={{}}
        className="px-6 py-4 font-Mulish font-semibold border rounded-lg text-white hover:bg-bg-div hover:text-bg-color border-bg-div "
      >
        {name}
      </a>
    </div>
  );
};

export default Button;
